/**
 * Embedded OAuth 2 Server implementation using JWT Tokens.
 *
 * The OAuth server authenticates a user/password login and if valid, returns a JWT Token which
 * can be used by clients to authorize API access.
 *
 * Note that if you have multiple services, you should use a dedicated OAuth server.
 *
 */
package com.elderbyte.oauth.server;
